#!/usr/bin/env bash
set -euo pipefail

ROOT="${ROOT:-$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)}"
export ROOT TASK=docvqa
export DOCVQA_PHYSICAL_GPU_IDS="${DOCVQA_PHYSICAL_GPU_IDS:-auto}"
export DOCVQA_GPU_UUIDS="${DOCVQA_GPU_UUIDS:-}"
export N_GPUS_PER_NODE="${N_GPUS_PER_NODE:-4}"
export RAY_NUM_CPUS="${RAY_NUM_CPUS:-16}"
export AGENT_LOOP_WORKERS="${AGENT_LOOP_WORKERS:-4}"
export TRAIN_BATCH_SIZE="${TRAIN_BATCH_SIZE:-4}"
export PPO_MINI_BATCH_SIZE="${PPO_MINI_BATCH_SIZE:-4}"
export ROLLOUT_N="${ROLLOUT_N:-8}"
export TOTAL_EPOCHS="${TOTAL_EPOCHS:-15}"
export DATA_SEED="${DATA_SEED:-42}"
export MAX_MODEL_LEN="${MAX_MODEL_LEN:-131072}"
export MAX_RESPONSE_LENGTH="${MAX_RESPONSE_LENGTH:-32768}"
export MAX_TURN_RESPONSE_LENGTH="${MAX_TURN_RESPONSE_LENGTH:-512}"
export GPU_MEMORY_UTILIZATION="${GPU_MEMORY_UTILIZATION:-0.50}"
export MAX_USER_TURNS="${MAX_USER_TURNS:-50}"
export MAX_ASSISTANT_TURNS="${MAX_ASSISTANT_TURNS:-50}"
export SAVE_FREQ="${SAVE_FREQ:-1}"
export MAX_ACTOR_CKPT_TO_KEEP="${MAX_ACTOR_CKPT_TO_KEEP:-1}"
export VERL_PROTECTED_CHECKPOINT_STEPS="${VERL_PROTECTED_CHECKPOINT_STEPS:-60,120,180}"
export TEST_FREQ="${TEST_FREQ:--1}"
export VAL_BEFORE_TRAIN="${VAL_BEFORE_TRAIN:-False}"
export MULTI_TURN_FORMAT="${MULTI_TURN_FORMAT:-paper_react_cli}"
exec "$ROOT/scripts/trace2skill/run_verl_agentic_rl.sh" "$@"
