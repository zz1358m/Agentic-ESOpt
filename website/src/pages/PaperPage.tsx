import { PaperByline } from "../components/PaperByline";
import { PageFrame } from "../components/SiteChrome";
import { SectionHeading } from "../components/TaskPrimitives";
import { paper, paperCitation } from "../paper";
import { siteHref } from "../site";

export function PaperPage(){return <PageFrame><section className="paper-page section-shell"><div className="paper-page__title"><span className="eyebrow">Research paper · Agentic ESOpt</span><h1 className="display-title" aria-label={paper.title}><span className="hero-title__line">Fine-Tuning </span><span className="hero-title__line hero-title__agents">Long-Horizon LLM Agents <small>with</small>{" "}</span><span className="hero-title__line hero-title__memory"><em>Minimal GPU Memory Requirements.</em></span></h1><p>Agentic ESOpt uses population-level black-box feedback to fine-tune the full parameters of tool-using and interactive agents.</p><PaperByline/><div><a className="button button--primary" href={siteHref("paper/agentic-esopt.pdf")} target="_blank" rel="noreferrer">Open PDF ↗</a><a className="button button--ghost" href={paper.githubUrl}>View code ↗</a></div></div><div className="paper-summary"><SectionHeading eyebrow="At a glance" title="One optimization interface, five environments."/><div className="paper-points"><article className="paper-point"><span>01</span><h3>Why ES?</h3><p>Full-parameter updates without storing rollout activations or optimizer states.</p></article><article className="paper-point"><span>02</span><h3>What is optimized?</h3><p>The model behind the agent; existing environments, tools, and outer loops stay intact.</p></article><article className="paper-point"><span>03</span><h3>Where is it tested?</h3><p>Sudoku, Math, DocVQA, WebArena-Lite, and automatic heuristic design.</p></article></div></div><div className="citation-card"><span className="eyebrow">Citation</span><pre>{paperCitation}</pre></div></section></PageFrame>}
